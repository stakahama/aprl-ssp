exit
exit
exit
exit
exit
filenames
zip(filenames['baselined'],filenames['fitplots'])
exit
import os

figpath = '../../programs/ftir/outputs'

filenames = {'fitplots':[],'baselined':[]}
for f in os.listdir(figpath):
    if 'fitplot_' in f:
        filenames['fitplots'].append(f)
    elif 'baseline_' in f:
        filenames['baselined'].append(f)

PATT = r'\includegraphics[height=.45\textheight]{{\FTIRPATH/outputs/{:s}}}\\'
def includegraphics(pair):
    text = r'\begin{center}' + '\n' +\
        PATT.format(pair[0]) + '\n' +\
        PATT.format(pair[1]) + '\n' +\
        r'\end{center}' + '\n\n'
    return text

filenames['baselined'][0]
exit
echo "hi" | perl -e 'print "it is a pipe" if -p STDIN'
exit
?write.csv
q()
getwd()
data.frame("a","b")
data.frame("a"=1,"b"=2)
write.table(data.frame("a"=1,"b"=2),"test.tsv",sep="\t")
q()
